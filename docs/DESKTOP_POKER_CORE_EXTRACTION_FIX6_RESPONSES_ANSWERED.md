# FIX6 Responses — DESKTOP_POKER_CORE_EXTRACTION_FIX6_SPEC.md / _TODO.md

Fill in each `A:` line, then share this file back (or paste your answers).

---

1. Q: **P0.1 strictness** — Tests are currently 252/252 with no persistence noise. The `afterEach` teardown for `window.__TAURI_INTERNALS__` / `window.__TAURI__` in `setup.ts` is currently missing. Do you want this added as a defensive measure regardless of whether the symptom is currently visible, or only if we can reproduce the noise first?
   A: Add the defensive `afterEach` teardown regardless of whether the noise is currently reproduced. The symptom was real in the previous review and this is cheap test hygiene. The teardown should delete both `window.__TAURI_INTERNALS__` and `window.__TAURI__` after every test so one test cannot leak a fake Tauri runtime into the next test. Do not suppress `console.error`; make the environment clean instead.

2. Q: **P0.3 health diagnostic destination** — When `sync_snapshots_after_lobby_mutation` fails (i.e., the NPC add mutation succeeded but broadcasting the updated snapshot to connected clients failed), should the error be: (a) recorded on `HostRuntimeHealth` so the frontend can surface it via `get_host_session_status`, or (b) emitted as a `tracing::warn!` and otherwise swallowed silently?
   A: Use option (a). Record the failure on `HostRuntimeHealth` and expose it through the host/session status path. Do not use `tracing::warn!` as the only behavior, and do not silently swallow this. The authoritative lobby mutation already succeeded, so the caller should not receive a plain mutation failure that implies rollback happened. Treat snapshot sync failure as a post-mutation diagnostic: record a health counter/last_error, return success for the mutation if the host state is valid, and let the debug/status UI surface that clients may need resync.

3. Q: **P0.4 `client_connect.rs` timeout errors** — If `set_read_timeout` or `set_write_timeout` fails (this is OS-level and very rare in practice), should we: (a) propagate the error as a hard connection failure, or (b) log and continue connecting anyway?
   A: Use option (a): propagate the timeout setup failure as a hard connection failure. A stream without the intended read/write timeouts can hang in paths that assume bounded waits, so continuing changes runtime semantics. Return an actionable `NetworkingError` that says whether read or write timeout setup failed. If a future platform genuinely cannot support these timeouts, add an explicit platform-specific policy and test it; do not log-and-continue by default.

4. Q: **P2.1–P2.5 already done** — The poker-core workspace extraction (workspace Cargo.toml, module migration, PokerEngine facade, ANDROID_ARCHITECTURE.md, purity audit) was fully completed in the FIX5 session. Should these be treated as done (quick verification pass only), or do you want a fresh review and re-check of each item?
   A: Do a fresh review and re-check of each P2.1-P2.5 item. Do not assume the workspace extraction is done just because a previous session says so. If the current repo already contains the root workspace, `crates/poker-core`, moved modules, facade, Android architecture doc, and passing purity audit, then the task can be marked done after verification. But the acceptance criteria still require checking each item explicitly: `cargo metadata`, `cargo test -p poker-core`, `cargo tree -p poker-core`, import paths, absence of Tauri/networking/platform dependencies in `poker-core`, and the Android/Kotlin networking-boundary doc. If any of those are missing or only partially done, fix them in this pass.
